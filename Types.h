#ifndef TYPES_H
#define TYPES_H

enum class VehicleType {
    Ground,
    Air
};

enum class RaceType {
    GroundOnly,
    AirOnly,
    Mixed
};

enum class VehicleName {
    Camel,
    FastCamel,
    Centaur,
    Boots,
    MagicCarpet,
    Eagle,
    Broom
};

#endif